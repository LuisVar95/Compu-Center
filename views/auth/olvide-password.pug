extends ../layout/admin

block contenido
    main.login.contenedor
        h1.login__heading= pagina
        form.login__formulario(method="POST" action="/auth/olvide-password")
            div.login__campo
                label.login__label Email:
                input.login__input(type='email', placeholder='Tu email' name="email")
            div.login__submit
                button.login__boton Enviar Instrucciones
        nav.login__nav
            a.login__nav-enlace(href='/auth/registro') ¿No tienes una cuenta aún? Crear cuenta
            a.login__nav-enlace(href='/auth/login') ¿Ya tienes una cuenta? Iniciar sesión